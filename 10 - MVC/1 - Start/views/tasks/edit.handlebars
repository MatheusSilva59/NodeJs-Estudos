<h1>Editing a Task</h1>
<form class="main-form" action="/tasks/update" method="post">
    <input type="hidden" name="id" value="{{task.id}}">
    <div class="form-control">
        <label for="title">Title: </label>
        <input type="text" name="title" id="title" placeholder="What is the title name?" value="{{task.title}}">
    </div>
    <div class="form-control">
        <label for="description">Description: </label>
        <textarea name="description" placeholder="Describe your task">{{task.description}}</textarea>
    </div>
    <input type="submit" value="Edit">
</form>